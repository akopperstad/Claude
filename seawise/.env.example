# Lead delivery — set these to activate contact-form emails.
# Without SMTP_URL, leads are logged server-side and the form still succeeds.
SMTP_URL=smtp://user:password@smtp.yourhost.com:587
LEADS_TO=hello@seawise.no
LEADS_FROM=hello@seawise.no
